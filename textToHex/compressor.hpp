#include <iomanip>
#include <iostream>
#include <vector>

std::vector<int> compressor(std::string fileString);
std::string temmieTransform(std::string fileInput);
std::string burrowsWheeler(std::string fileInput);
std::string reverter(std::string fileInput);